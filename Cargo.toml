[package]
edition = "2024"
name = "litelemetry"
repository = "https://github.com/smartcrabai/litelemetry"
version = "0.1.0"

[dependencies]
tokio = { version = "1.50.0", features = ["rt", "macros"] }

[profile.release]
codegen-units = 1
lto = true
strip = true
